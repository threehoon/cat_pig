# 后期能力：助手、问诊、养宠经验

对标截图里没有这些。已批准要做，但**不是现在的主路径**。对标仍以 [benchmark.md](benchmark.md) 上表为准；这三块的模块边界、预约名、产品接口只认本文件。

阶段 F 已接「小x」的页面和 mock。真向量库、RAG、问诊、养宠经验都等各自开工。**不预建 `consult` / `experience` 空目录。**

## 三块三模块

| 英文目录 / API feature | 用户看见 | 何时 | 和谁分开 |
|---|---|---|---|
| `assistant` | 底栏中间「小x」，进对话页；站内 AI 助手 | mock 已接；向量库 / RAG 后接 | 对话不进广场帖 |
| `consult` | 问诊（结构化症状 → 建议） | 后期 | 不是助手里的一种聊天；不是广场帖 |
| `experience` | 用户分享养宠经验 | 后期 | 不是 `Post.board`，不是动态流的一种分类 |

预约名已写入 [handoff.md](../handoff.md)。禁止用 `ai`、`rag`、`doctor`、`forum`、`plaza` 当模块目录名。

## 边界

- 广场（`community`）只做公开动态。助手入口在底栏「小x」，不在广场。对话、病历、经验正文都不进 `Post`。
- 助手回答里的「相近内容」只带摘要（id + 标题/正文/封面）。点进去走对方页面路由。现在只挂已发布帖；经验模块落地后再扩，不把经验行写进帖子表。
- 问诊需要生成文字时，只调 `assistant` 的公开 service，不在 `consult` 里再接一套模型或向量库。
- 向量库、切片、embedding、LLM、工具调用、模型密钥只放 `assistant` 服务端。小程序只打助手自己的 HTTP 合同，不选模型、不调工具。
- 跨模块只走对方公开 service；页面不 import 另一模块的 `services`（`media` 除外，见 [modules.md](../framework/modules.md)）。

## 预留的产品接口

路径和字段只认 [../api/contract.md](../api/contract.md)。合同里出现的 path，mock 就必须实现。

开工时按下面的资源加，不要另起一套。

### assistant

| 资源 | 小程序要什么 | 以后真后端怎么接 |
|---|---|---|
| 推荐问题 | 空态几条可点的问题 | 可配置；仍走列表信封 |
| 提问 | `question` + `conversation_id`（可空）→ `answer`、`source`、`citations`、相近内容 | 见下方「回答流水」。打分、改写问法、向量库、LLM、工具都在 service 内，不新开小程序 path |
| 说明书条目 | mock 可内置；小程序第一刀不必有百科列表页 | 以后知识库管理可另开管理端，不塞进广场 |

`source` 取值锁成 `knowledge` / `search` / `generated`。`citations` / 相近内容始终是数组（可 `[]`）。

## 回答流水（行为规范）

小程序始终只 `POST /ask`。用户只看到**一条**回答。相关度打分、问法改写、工具调用都在 `assistant` 服务端。**改的是拿去搜知识库的问法，不是内部先生成几套答案再挑。**

不得：用「向量库有没有返回行」当失败；把不合格段落塞进生成；小程序接 LLM 或工具；工具无限轮；症状/用药走 `generated` 编诊断或药名。

### 步骤

1. 用用户原问题检索向量库（top-k）。几乎总会返回几段，空结果很少见。
2. **相关度门**（不是向量相似度）：每段能不能当本题证据。分三档：够用 / 含糊 / 不行。
   - **够用**：只用过线段落生成 → `source = knowledge`，带 `citations`。
   - **含糊 / 不行**：丢掉这批段落，不进入提示词。
3. 不够用时，按原问题**内部改写检索问法**（用户不可见），对**同一知识库**再搜，最多再 1～2 次。够用了仍是 `knowledge`。
4. 改写后仍不够用 → 才调 LLM，并只给有限工具（联网搜；以后可加站内经验检索）。工具最多 2 轮。
   - 工具有能用的资料 → 按资料答，`source = search`。
   - 工具也没有，且是日常常识 → 模型用自身知识答，加「仅供参考」，`source = generated`。
   - 工具也没有，且像看病/用药 → **拒答**：说明知识库没有、建议去医院，不编诊断、药名、剂量。
5. 相近广场帖始终附加在回答下，**不参与打分、不当证据**。

问诊若要生成文字，只调助手这一条流水，不另接一套模型。

第一刀 mock 用关键词假装 `knowledge` / `search` / `generated` 三档，不真打分、不接模型。看病/用药走固定拒答，`source` 仍为 `generated`（不新增第四档）。接口形状以后不变。相近帖 mock 取已发布帖摘要，不当证据。

### consult（后期）

结构化问诊单、结果、免责声明。请求体不是助手的 `question` 字符串。病历不进 `community`，也不进助手会话表。

### experience（后期）

经验条目的列表 / 详情 / 发布。互动若与帖子相同（点赞评论），走自己的表或显式复用 community 公开方法，禁止把经验当 `Post` 插入。

## 助手这一刀（已接 mock）

形态已确认并落地。模块按 [adding-a-module.md](../framework/adding-a-module.md) 建在 `assistant`。

- 底栏中间 C 位「小x」→ `modules/assistant/pages/chat/chat`（tab）。广场不加入口。文案用「小x」，不用「问问」「养宠问答」。
- 对话页：空态问候 + 推荐问题；对话气泡；来源芯片；命中说明书时挂引用；下面最多 2 条相近广场帖。
- 阶段 F：mock 用关键词假装 `knowledge` / `search` / `generated`。不接真向量库、不接搜索工具、不搭 FastAPI、不建 `consult` / `experience`。
- 助手是站内通用问答，不限于宠物百科。回答可写「仅供参考」。开药、下诊断留给 `consult`，第一刀不做问诊表单。
- 图生视频不占底栏，从首页四入口进创作页。

## 问诊（后期才做）

自己的问诊产品，**不**对标「萌爪医生」品牌、插画、文案。不做开药、不替代医院。商城 / 上门 / 活体仍不做。

## 仍不做

随手记、养宠账单、事项提醒、宠物档案 tab、商城、上门、活体。见 [benchmark.md](benchmark.md) 下表。
